<?xml version="1.0" encoding="UTF-8" ?>

<container xmlns="http://www.symfony-project.org/schema/dic/services"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.symfony-project.org/schema/dic/services http://www.symfony-project.org/schema/dic/services/services-1.0.xsd">

    <parameters>
        <parameter key="doctrine_user.auth.class">Bundle\DoctrineUserBundle\Auth</parameter>
        <parameter key="doctrine_user.auth.session_path">doctrine_user/auth/identifier</parameter>
        <parameter key="doctrine_user.auth.user_identifier">id</parameter>
    </parameters>

    <services>

        <service id="doctrine_user.auth" class="%doctrine_user.auth.class%" shared="true">
            <argument type="service" id="doctrine_user.user_repo" />
            <argument type="service" id="session" />
            <argument type="collection">
                <argument key="session_path">%doctrine_user.auth.session_path%</argument>
                <argument key="user_identifier">%doctrine_user.auth.user_identifier%</argument>
            </argument>
        </service>

    </services>

</container>
