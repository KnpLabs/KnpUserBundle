{% form_theme form form.getOption('theme') %}
<form action="{{ path('fos_user_group_create') }}" {{ form|render_enctype }} method="POST" class="fos_user_group_new">
    {{ form|render }}
    <div>
        <input type="submit" value="{% trans 'Create group' from 'FOSUserBundle' %}" />
    </div>
</form>
