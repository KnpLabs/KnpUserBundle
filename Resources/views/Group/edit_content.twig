{% form_theme form form.getOption('theme') %}
<form action="{{ path('fos_user_group_update', {'groupname': groupname} ) }}" {{ form_enctype(form) }} method="POST" class="fos_user_group_edit">
    {{ form_field(form) }}
    <div>
        <input type="submit" value="{% trans 'Update group' from 'FOSUserBundle' %}" />
    </div>
</form>
