{% form_theme form form.getOption('theme') %}
<form action="{{ path('fos_user_group_update', ['groupname': groupname] ) }}" {{ form|render_enctype }} method="POST" class="fos_user_group_edit">
    {{ form|render }}
    <div>
        <input type="submit" value="{% trans 'Update group' from 'FOSUserBundle' %}" />
    </div>
</form>
