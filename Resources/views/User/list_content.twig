<div class="doctrine_user_user_list">
    <ul>
    {% for user in users %}
        <li><a href="{% path 'doctrine_user_user_show' with ['username': user.getUsername()] %}">{{ user.getUsername() }}</a></li>
    {% endfor %}
    </ul>
</div>
