{% form_theme form form.getOption('theme') %}
<form action="{{ path('fos_user_user_create') }}" {{ form|render_enctype }} method="POST" class="fos_user_user_new">
    {{ form|render }}
    <div>
        <input type="submit" value="{% trans 'Create user' from 'FOSUserBundle' %}" />
    </div>
</form>
