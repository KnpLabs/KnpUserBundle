{% form_theme form 'FOSUserBundle::form.html.twig' %}
<form action="{{ path('fos_user_user_edit', {'username': username} ) }}" {{ form_enctype(form) }} method="POST" class="fos_user_user_edit">
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'user.edit.submit'|trans([], 'FOSUserBundle') }}" />
    </div>
</form>
