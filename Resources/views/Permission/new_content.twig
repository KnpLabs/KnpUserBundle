{% form_theme form form.getOption('theme') %}
<form action="{% path 'doctrine_user_permission_create' %}" {{ form|render_enctype }} method="POST" class="doctrine_user_permission_new">
    <ul>
        {% for field in form.getAllVisibleFields() %}
            {{ field|render }}
        {% endfor %}
        <li>
            <input type="submit" value="{% trans 'Create permission' from 'DoctrineUserBundle' %}" />
        </li>
    </ul>
    {{ form|render_hidden }}
</form>
